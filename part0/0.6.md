```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>server: Create new note
    server-->>browser: New note created
    deactivate server

    Note right of browser: The browser renders the new note without saving it on the server


```